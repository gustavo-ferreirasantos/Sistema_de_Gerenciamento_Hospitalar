<section id="agendar" class="secao d-none">
    <div class="card p-4">
      <h4 class="mb-3"><i class="bi bi-calendar-plus me-2"></i>Agendar Atendimento</h4>

      <form id="formAgendamento">
        <div class="row mb-3">
          <!-- Tipo principal -->
          <div class="col-md-4">
            <label for="tipoAtendimento" class="form-label">Tipo de Atendimento</label>
            <select id="tipoAtendimento" class="form-select" required>
              <option selected disabled>Selecione...</option>
              <option value="exame">Exame</option>
              <option value="procedimento">Procedimento</option>
              <option value="consulta">Consulta</option>
            </select>
          </div>

          <!-- Tipo específico -->
          <div class="col-md-4">
            <label for="tipoEspecifico" class="form-label">Tipo</label>
            <select id="tipoEspecifico" class="form-select" required disabled>
              <option selected disabled>Selecione o tipo...</option>
            </select>
          </div>

          <!-- Médico -->
          <div class="col-md-4">
            <label for="medico" class="form-label">Médico Responsável</label>
            <select id="medico" class="form-select" required disabled>
              <option selected disabled>Selecione o médico...</option>
            </select>
          </div>
        </div>

        <!-- Data e hora -->
        <div class="row mb-3">
          <div class="col-md-6">
            <label for="data" class="form-label">Data</label>
            <input type="date" id="data" class="form-control" required>
          </div>
          <div class="col-md-6">
            <label for="hora" class="form-label">Hora</label>
            <input type="time" id="hora" class="form-control" required>
          </div>
        </div>

        <button type="submit" class="btn btn-hospital mt-3">
          <i class="bi bi-check2-circle me-2"></i>Confirmar Agendamento
        </button>
      </form>
    </div>
  </section>